# Howly

A lightweight Markdown to HTML converter written in Ruby.

## To-Do:

- [ ] Automatically add paragraphs
- [ ] Find and replace headings
- [x] Find and replace bold text: `**bold**` or `__bold__`
- [x] Find and replace italic text: `*italic*` or `_italic_`
- [ ] Find and replace both bold and italic at the same time: `***boldanditalic***` `___boldanditalic___`
- [x] Find and replace hyperlinks
- [ ] Embed YouTube links if found by themselves on a line
- [ ] Replace Markdown ordered lists with HTML. Check for usage of 1), 2), etc. as well
- [ ] Take unordered lists into account as well. The user might use `-`, `*`, `+` or `-` to indicate an element in such a list. Bear in mind that indented items are also a possibility. Additionally, unordered lists can start with numbers
- [ ] Allow for images
- [ ] Allow for code blocks
